/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    output: 'standalone', // This should NOT be 'export'
    // other config options...
  }
